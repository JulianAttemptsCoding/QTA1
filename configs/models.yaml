# Candidate open-weight agent models. cutoff = documented training cutoff (VERIFY each
# model card at G0; the OOS window must start strictly after the max cutoff used).
# T4 (16 GB) fit: prefer <= 4B params or 4-bit quants of 7-8B.
models:
  - id: Qwen/Qwen2.5-1.5B-Instruct
    cutoff: "VERIFY"
  - id: Qwen/Qwen2.5-3B-Instruct
    cutoff: "VERIFY"
  - id: meta-llama/Llama-3.2-3B-Instruct
    cutoff: "2023-12 (VERIFY on model card)"
  - id: microsoft/Phi-3.5-mini-instruct
    cutoff: "VERIFY"
  - id: google/gemma-2-2b-it
    cutoff: "VERIFY"
sampling:
  temperatures: [0.7, 1.0]
  seeds_per_persona: 1
