name: agorasim
channels: [conda-forge]
dependencies:
  - python=3.11
  - numpy>=1.26
  - pandas>=2.1
  - pydantic>=2.6
  - requests>=2.31
  - pyyaml>=6.0
  - pytest>=8.0
  # vllm is intentionally NOT here: it lives only in the Vertex worker image.
