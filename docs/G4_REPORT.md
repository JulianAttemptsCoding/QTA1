# G4 P4 Budget Checkpoint

- Decision: **PASS**
- Completed outputs: `125,000` / `125,000`.
- Aggregate worker time: `32.200` hours.
- Estimated checkpoint spend: `$9.66`.
- G0 baseline cost/decision: `$0.00006320`.
- P4 observed cost/decision: `$0.00007728`.
- Relative overrun: `22.3%` (pause threshold `30%`).

The remaining P4 main jobs may proceed only when the decision is `PASS`; a `PAUSE_REPLAN` result must be documented before additional inference.
