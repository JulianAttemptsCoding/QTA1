substitutions:
  _IMAGE_URI: us-central1-docker.pkg.dev/project-82d97cf9-5889-43a4-850/agorasim/worker:latest

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - docker/Dockerfile.worker
      - -t
      - ${_IMAGE_URI}
      - .

images:
  - ${_IMAGE_URI}
